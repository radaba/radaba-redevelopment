# Notification Center

Radaba provides authenticated in-app notifications through a global bell and `/home/notifications`. The bell loads seven recent records; the page uses 25-record stable cursor pages. External email, SMS, WhatsApp, device push, preferences, digests, deletion and migrations are outside this milestone.
