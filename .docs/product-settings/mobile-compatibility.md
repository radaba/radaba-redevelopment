# Mobile compatibility

Android/mobile APIs and RTDB operational shapes are unchanged. Upload limits, Assignment lifecycle behavior, notification behavior, timezone conventions, and mobile security mode may be duplicated or assumed by mobile clients; they remain deployment-managed and require coordinated review before alteration.

A future maintenance mode or runtime settings root must define mobile API fallback and compatibility behavior before implementation. Feature availability cards never replace server authorization.
