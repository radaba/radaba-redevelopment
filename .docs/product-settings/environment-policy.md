# Environment policy

The page displays only Development, Test, or Production. It does not expose project identifiers, endpoints, buckets, build secrets, or raw environment variables. Deployment-managed values require restart or redeployment. No development or production configuration can be mutated through this page.

Firebase integration status indicates configuration presence, not a live health probe. Maintenance mode and read-only mode are reported as unavailable because no approved enforcement path exists.
