# Global Search

Global Search adds an authenticated header command palette and `/home/search`. It searches only entities enabled by the verified user's exact canonical privileges. Results are normalized in memory and never persisted. Ctrl+K and Cmd+K open the palette; the full page provides authorized filters, bounded counts, stable load-more cursors, deep links, and partial-source warnings.
