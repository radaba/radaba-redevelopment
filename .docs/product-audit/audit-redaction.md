# Redaction

Presentation and export recursively redact password, secret, token, authorization, private-key, service-account, credential, API-key, bytes, and base64 fields. Token-bearing strings are redacted. Stored audit records are never modified. React renders values as text; raw HTML is not used.