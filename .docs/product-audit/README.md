# Administrator Audit Center

`/home/admin/audit` is a strictly authorized, read-only investigation workspace. It normalizes existing audit data in application memory and never writes a replacement audit schema.

The UI includes bounded-result summaries, filters, responsive event lists, composite-source details, safe before/after comparison, contextual links, partial-source warnings, and CSV export of the current filtered loaded set.