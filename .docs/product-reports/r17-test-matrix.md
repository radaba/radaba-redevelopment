# R17 test matrix

Tests cover IDs, Storage parsing, missing/malformed fields, unexpected paths, raw date handling,
duplicate references, bounded read-only repository source, authorization, list/detail states,
responsive UI, navigation, PDF actions, export exclusions, and mobile/report-write regressions.
