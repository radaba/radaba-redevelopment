# R17 UI design

The list uses URL-driven server filters, bounded summaries, a sticky desktop table, mobile
cards, column visibility, metadata export, and safe PDF actions. Detail uses side-by-side
metadata/browser-native PDF preview on desktop and stacked content on mobile.
