# R17 authorization

List and detail Server Components require a revocation-aware Active user with strict existing
`/assignment` privilege before repository access or rendering usable PDF actions. Existing web
Assignment authorization is page-scoped; no unproven coordinator/rigger object policy is added.
