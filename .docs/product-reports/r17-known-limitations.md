# R17 known limitations

- The global center is a bounded recent Assignment window, not a dataset-wide total.
- The persisted model supports one current report pair per Assignment.
- Browser-native iframe preview depends on browser PDF support and direct tokenized URL access.
- R17 does not fetch PDF binaries to verify Storage existence.
- Object-level rigger/coordinator scoping awaits an approved web authorization contract.
