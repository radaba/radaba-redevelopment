# R15C UI design

The single route provides Overview, Sectors & Cells, Images, People, Recorded Timestamps, and
Data Quality sections with sticky in-page navigation. Desktop uses a dense matrix; mobile uses
sector-group and band cards. Images lazy-load and local filters operate only on the bounded
aggregate. Actions are navigation, refresh, safe map opening, and token-free metadata export.
