import { FirebaseMobileClientAuthentication } from "@/server/mobile-api/authentication/firebase-client-adapter"; import { createResetPasswordHandler } from "@/server/mobile-api/routes/auth-profile-handlers.mjs"; import { createMobileResetPasswordService } from "@/server/mobile-api/services/mobile-auth-profile-services.mjs";
export const dynamic = "force-dynamic"; const handler = createResetPasswordHandler(createMobileResetPasswordService(new FirebaseMobileClientAuthentication())); export { handler as GET, handler as POST, handler as PUT, handler as PATCH, handler as DELETE, handler as OPTIONS, handler as HEAD };
