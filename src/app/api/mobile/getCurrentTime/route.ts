import { getCurrentTime } from "@/server/mobile-api/routes/get-current-time.mjs";

export const dynamic = "force-dynamic";

export const GET = getCurrentTime;
export const POST = getCurrentTime;
export const PUT = getCurrentTime;
export const PATCH = getCurrentTime;
export const DELETE = getCurrentTime;
export const OPTIONS = getCurrentTime;
export const HEAD = getCurrentTime;
