import{redirect}from"next/navigation";import{resolveAuthenticatedUser}from"@/services/authentication/auth";import{globalSearchAuthorizedEntities}from"@/features/search/global-search-authorization";import{GlobalSearchPage}from"@/components/search/global-search-page";export const dynamic="force-dynamic";export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){let user;try{user=await resolveAuthenticatedUser()}catch{redirect("/login")}const entities=globalSearchAuthorizedEntities(user);if(!entities.length)return <section className="rounded-2xl border border-amber-200 bg-amber-50 p-6"><h1 className="text-xl font-semibold">Permission denied</h1><p className="mt-2 text-sm">You do not have access to searchable Radaba entities.</p></section>;const value=(await searchParams).q,query=Array.isArray(value)?value[0]??"":value??"";return <GlobalSearchPage initialQuery={query.slice(0,160)} authorizedEntities={entities}/>}