import Link from"next/link";import type{SystemSettingDefinition,SystemSettingSection}from"@/features/settings/system-settings";
const labels:Record<SystemSettingSection,string>={general:"General",assignment:"Assignment",security:"Security",uploads:"Uploads",notifications:"Notifications",reports:"Reports",maintenance:"Maintenance",integrations:"Integrations and environment",history:"Change history"};
const tone=(status:string|null)=>status==="connected"?"bg-emerald-50 text-emerald-800":status==="unavailable"?"bg-amber-50 text-amber-800":"bg-slate-100 text-slate-700";
export function SystemSettingsWorkspace({groups,environment}:{groups:{section:SystemSettingSection;items:SystemSettingDefinition[]}[];environment:string}){return <div className="space-y-6"><header className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-sm font-semibold text-indigo-700">Administration</p><h1 className="text-3xl font-bold text-slate-950">System Settings</h1><p className="mt-2 text-slate-600">Manage global Radaba configuration and operational defaults.</p></div><div className="flex items-center gap-2"><span className="rounded-full bg-indigo-50 px-3 py-1 text-xs font-semibold text-indigo-800">{environment}</span><Link href="/home/settings" className="rounded-lg border bg-white px-3 py-2 text-sm focus-visible:ring-2 focus-visible:ring-indigo-500">Refresh</Link></div></header><div role="status" className="rounded-xl border border-blue-200 bg-blue-50 p-4 text-sm text-blue-900"><b>No runtime-editable settings are approved.</b> Values below are deployment-managed or read-only. No unsaved changes, review, or save action is available.</div><nav aria-label="Settings sections" className="flex gap-2 overflow-x-auto pb-1">{groups.map(group=><a key={group.section} href={`#${group.section}`} className="whitespace-nowrap rounded-lg border bg-white px-3 py-2 text-sm focus-visible:ring-2 focus-visible:ring-indigo-500">{labels[group.section]}</a>)}</nav>{groups.map(group=><section id={group.section} key={group.section} aria-labelledby={`${group.section}-heading`} className="scroll-mt-24 rounded-2xl border bg-white p-5 shadow-sm"><div className="mb-4"><h2 id={`${group.section}-heading`} className="text-lg font-semibold">{labels[group.section]}</h2>{group.section==="maintenance"?<p className="mt-1 text-sm text-amber-700">Maintenance mode is not configured and cannot be enabled from this page.</p>:null}</div><div className="divide-y">{group.items.map(item=><article key={item.key} className="grid gap-3 py-4 sm:grid-cols-[minmax(0,1fr)_minmax(12rem,0.7fr)]"><div><div className="flex flex-wrap items-center gap-2"><h3 className="font-medium text-slate-950">{item.label}</h3>{item.highRisk?<span className="rounded-full bg-red-50 px-2 py-0.5 text-xs font-semibold text-red-800">Protected</span>:null}</div>{item.description?<p className="mt-1 text-sm text-slate-600">{item.description}</p>:null}<p className="mt-1 text-xs text-slate-500">{item.mobileCompatibility}</p></div><div className="sm:text-right"><p className="break-words font-medium">{item.value}</p><span className={`mt-1 inline-block rounded-full px-2 py-0.5 text-xs font-medium ${tone(item.status)}`}>{item.classification==="deployment"?"Managed by deployment":"Read-only"}</span><p className="mt-1 text-xs text-slate-500">{item.management}{item.requiresRestart?" Restart or redeployment required.":""}</p></div></article>)}</div>{group.section==="history"?<Link href="/home/admin/audit" className="mt-4 inline-flex rounded-lg border px-3 py-2 text-sm font-medium text-indigo-700 focus-visible:ring-2">Open Administrator Audit Center</Link>:null}</section>)}</div>}
