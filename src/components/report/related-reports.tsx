import Link from"next/link";import type{AorReportRecord}from"@/features/report/aor-report-types";const show=(x:unknown)=>String(x??"").trim()||"—";export function RelatedReports({reports,title="AOR Reports"}:{reports:AorReportRecord[];title?:string}){return <section className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="font-semibold">{title}</h2>{reports.length?<div className="mt-4 space-y-3">{reports.map(report=><article className="flex flex-col gap-3 rounded-xl border p-4 sm:flex-row sm:items-center sm:justify-between" key={report.reportId}><div className="min-w-0"><h3 className="truncate font-semibold">{show(report.fileName??report.towerId)}</h3><p className="mt-1 text-xs text-slate-500">{show(report.closedDateDisplay)} · {show(report.assignmentId)} · {show(report.assignmentCategory)} · {report.qualityStatus}</p><p className="mt-1 text-xs text-slate-500">{show(report.company)} · {show(report.coordinatorName)} · {show(report.riggerName)}</p></div><div className="flex flex-wrap gap-2"><Link className="row-link" href={`/home/reports/${report.reportId}`}>View Detail</Link>{report.qualityStatus==="valid"&&report.downloadUrl?<><Link className="row-link" href={`/home/reports/${report.reportId}#pdf`}>Preview</Link><a className="row-link" href={report.downloadUrl} target="_blank" rel="noopener noreferrer" download>Download</a></>:<span className="text-xs text-amber-700">PDF unavailable</span>}</div></article>)}</div>:<p className="mt-3 text-sm text-slate-500">No report metadata is stored for this scope.</p>}</section>}
