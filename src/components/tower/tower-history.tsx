import Link from"next/link";import{towerAuditFieldLabel,type TowerAuditView}from"@/features/tower/tower-audit-contract";const actionLabel=(value:string)=>({tower_created:"Tower created",tower_updated:"Tower updated",tower_imported:"Tower imported"}[value]??value),sourceLabel=(value:string)=>({manual_create:"Manual create",manual_edit:"Manual edit",bulk_import:"Bulk import"}[value]??value),shown=(value:unknown)=>value===null||value===undefined||value===""?"Not set":typeof value==="boolean"?(value?"Yes":"No"):String(value);export function TowerHistory({towerKey,towerId,rows,nextCursor,pageSize,filters}:{towerKey:string;towerId:string;rows:TowerAuditView[];nextCursor:string|null;pageSize:number;filters:{action:string;source:string;field:string}}){const visible=rows.filter(row=>(!filters.action||row.action===filters.action)&&(!filters.source||row.source===filters.source)&&(!filters.field||row.changedFields.includes(filters.field))),created=rows.find(row=>row.action==="tower_created"||row.action==="tower_imported"),updated=rows.find(row=>row.action==="tower_updated");const href=(cursor:string|null)=>{const q=new URLSearchParams();q.set("pageSize",String(pageSize));if(cursor)q.set("cursor",cursor);for(const[k,v]of Object.entries(filters))if(v)q.set(k,v);return`/home/towers/${encodeURIComponent(towerKey)}/history?${q}`};return <div className="space-y-5"><div><Link href={`/home/towers/${encodeURIComponent(towerKey)}`} className="inline-flex min-h-11 items-center font-semibold text-indigo-700 focus-visible:ring-2">Back to Tower overview</Link><h1 className="text-2xl font-semibold">Tower History</h1><p className="text-sm text-slate-600">{towerId} · newest recorded events first</p></div><section aria-label="Recorded history summary" className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{[["Created at",created?.occurredAt],["Created by",created?.actorName||created?.actorEmail],["Last updated",updated?.occurredAt],["Recorded on this page",rows.length]].map(([label,value])=><article key={label} className="rounded-2xl border bg-white p-4"><p className="text-xs uppercase text-slate-500">{label}</p><p className="mt-2 break-words font-semibold">{shown(value)}</p></article>)}</section><form method="get" aria-label="Tower history filters" className="rounded-2xl border bg-white p-4"><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><Select name="action" label="Action" value={filters.action} options={["tower_created","tower_updated","tower_imported"]}/><Select name="source" label="Source" value={filters.source} options={["manual_create","manual_edit","bulk_import"]}/><label className="text-sm font-medium">Changed field<input name="field" defaultValue={filters.field} className="mt-1 min-h-11 w-full rounded-xl border px-3"/></label><label className="text-sm font-medium">Rows per page<select name="pageSize" defaultValue={pageSize} className="mt-1 min-h-11 w-full rounded-xl border px-3"><option>25</option><option>50</option><option>100</option></select></label></div><div className="mt-3 flex justify-end gap-2"><Link href={`/home/towers/${encodeURIComponent(towerKey)}/history`} className="inline-flex min-h-11 items-center px-3 font-semibold">Clear filters</Link><button className="min-h-11 rounded-xl bg-indigo-700 px-4 font-semibold text-white">Apply filters</button></div><p className="mt-2 text-xs text-slate-500">Filters apply to the current bounded history page.</p></form><p aria-live="polite" className="text-sm text-slate-600">Showing {visible.length} of {rows.length} recorded events on this page.</p>{!rows.length?<section className="rounded-2xl border border-dashed bg-white p-8 text-center"><h2 className="font-semibold">No recorded Tower history</h2><p className="mt-2 text-sm text-slate-600">No recorded Tower history is available. This Tower may predate audit tracking.</p></section>:!visible.length?<section className="rounded-2xl border border-dashed bg-white p-8 text-center"><h2 className="font-semibold">No matching recent history</h2><p className="mt-2 text-sm text-slate-600">Clear filters or inspect another bounded page.</p></section>:<ol aria-label="Tower change timeline" className="space-y-4">{visible.map(row=><li key={row.auditId}><article className="rounded-2xl border bg-white p-4 shadow-sm"><div className="flex flex-wrap justify-between gap-3"><div><h2 className="font-semibold">{actionLabel(row.action)}</h2><p className="text-sm text-slate-600">By {row.actorName||row.actorEmail||"Unknown actor"}{row.actorEmail&&row.actorName?` (${row.actorEmail})`:""}</p></div><div className="text-sm"><time dateTime={row.occurredAt}>{row.occurredAt?new Date(row.occurredAt).toLocaleString():"Unknown time"}</time><p>Source: {sourceLabel(row.source)}</p></div></div><details className="mt-4"><summary className="min-h-11 cursor-pointer font-semibold focus-visible:ring-2">{row.changedFields.length} changed {row.changedFields.length===1?"field":"fields"}</summary><dl className="mt-3 grid gap-3">{row.changedFields.map(field=><div key={field} className="rounded-xl bg-slate-50 p-3"><dt className="font-medium">{towerAuditFieldLabel(field)} <code className="text-xs">({field})</code></dt><dd className="mt-1 break-words text-sm"><span className="font-medium">Old value:</span> {shown(row.before[field])}</dd><dd className="break-words text-sm"><span className="font-medium">New value:</span> {shown(row.after[field])}</dd></div>)}</dl></details></article></li>)}</ol>}<nav aria-label="Tower history pagination" className="flex justify-between rounded-2xl border bg-white p-3"><Link href={`/home/towers/${encodeURIComponent(towerKey)}/history`} className="inline-flex min-h-11 items-center font-semibold text-indigo-700">Newest</Link>{nextCursor?<Link href={href(nextCursor)} className="inline-flex min-h-11 items-center font-semibold text-indigo-700">Older history</Link>:<span className="inline-flex min-h-11 items-center text-slate-500">No older recorded history</span>}</nav></div>}
function Select({name,label,value,options}:{name:string;label:string;value:string;options:string[]}){return <label className="text-sm font-medium">{label}<select name={name} defaultValue={value} className="mt-1 min-h-11 w-full rounded-xl border px-3"><option value="">All</option>{options.map(option=><option key={option} value={option}>{option.replaceAll("_"," ")}</option>)}</select></label>}
