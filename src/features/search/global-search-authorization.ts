import type{GlobalSearchEntity}from"./global-search";import{canAdministrate}from"@/features/admin/admin-authorization";const paths:Record<Exclude<GlobalSearchEntity,"user">,string>={assignment:"/assignment",tower:"/towers",cell:"/cells",rigger:"/riggers",report:"/reports"};export function globalSearchAuthorizedEntities(user:{role?:unknown;status?:unknown;privilege?:unknown}):GlobalSearchEntity[]{if(String(user.status).toLowerCase()!=="active"||typeof user.role!=="string")return[];const records=Array.isArray(user.privilege)?user.privilege:Object.values(user.privilege??{}),enabled=(path:string)=>records.some(record=>record&&typeof record==="object"&&(record as Record<string,unknown>).path===path&&(record as Record<string,unknown>)[user.role as string]===true),out:GlobalSearchEntity[]=(Object.entries(paths) as [Exclude<GlobalSearchEntity,"user">,string][]).filter(([,path])=>enabled(path)).map(([entity])=>entity);if(canAdministrate(user)&&enabled("/users"))out.push("user");return out}