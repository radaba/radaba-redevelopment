export function evaluateOperationalHealth({config,environment=process.env,compatibilityRouteCount=0}){const firebaseConfigured=Boolean(environment.NEXT_PUBLIC_FIREBASE_PROJECT_ID&&environment.NEXT_PUBLIC_FIREBASE_DATABASE_URL&&(environment.FIREBASE_SERVICE_ACCOUNT||environment.GOOGLE_APPLICATION_CREDENTIALS||environment.NODE_ENV!=="production"));const compatibilityReady=config.compatibilityApiEnabled&&compatibilityRouteCount===15;const blockers=[];if(!firebaseConfigured)blockers.push("firebase_configuration");if(!compatibilityReady)blockers.push("compatibility_subsystem");if(config.securityMode==="enforce"&&environment.NODE_ENV==="production")blockers.push("production_security_enforcement_unapproved");if(config.rolloutPercentage>0)blockers.push("production_rollout_unapproved");return {live:true,ready:blockers.length===0,status:blockers.length===0?"ready":"not_ready",checks:{firebase_configuration:firebaseConfigured,compatibility_subsystem:compatibilityReady,security_mode_safe:!blockers.includes("production_security_enforcement_unapproved"),rollout_inactive:config.rolloutPercentage===0},blockers};}